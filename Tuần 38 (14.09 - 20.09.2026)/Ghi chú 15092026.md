# 📝 Ghi chú ngày 15/09/2026 (Thứ Ba)

---

## 🎯 Mục tiêu trong ngày

- [ ] **[API-QA] - [QA-5920]:** Automation BE - Pending & History API endpoints (`GTO-15111`)
- [ ] **[API-QA] - [QA-5915]:** Automate BE - Poller Entity/Strategy Parsing (`GTO-15105`)

---

## 📝 Ghi chú công việc / Study

### 🧪 1. [API-QA] - [QA-5920]: Automation BE - Pending & History API endpoints (`GTO-15111`)

> [!NOTE]
> **Nhận diện loại Ticket:** Chứa từ khóa `endpoint` và tag `[API-QA]` ➔ Tự động áp dụng **Quy trình chuẩn 15 bước (Endpoint Automation Workflow)**.
> ⚙️ **Testing endpoint = sửa in project** (implement test code trực tiếp trong project repository).

1. **Create Test Scenario Prompt:** Tạo prompt yêu cầu AI phân tích test steps và tự động sinh Scenarios cho endpoint Pending & History.
2. **Create New Branch:** Tạo branch độc lập và chuyển status ticket sang `In Project`.
3. **Complete Scenario:** Rà soát và hoàn thiện các kịch bản kiểm thử (Happy Path, Negative Cases, Schema validation) trước khi viết code.
4. **Review Code:** Tự review code triển khai test script và các câu lệnh assert.
5. **Run Code:** Chạy test suite automation cục bộ (Pass 100%).
6. **Fix Code:** Sửa các lỗi phát sinh về assertion và kiểu dữ liệu trả về (nếu có).
7. **Create TestRail:** Tạo và cập nhật test cases lên hệ thống TestRail.
8. **Run Review Code:** Chạy lại toàn bộ test suite sau khi tạo TestRail để đảm bảo không có regression (Pass 100%).
9. **Check Orphan Functions:** Kiểm tra và dọn dẹp các orphan functions sau khi hoàn thành.
   > 💡 Sử dụng lệnh `/review-code` để AI tự động scan orphan functions.
10. **Kiểm tra model Pydantic:** Rà soát lại toàn bộ model Pydantic đảm bảo đúng schema.
11. **Commit and Push Code:** Commit với message chuẩn và push code lên remote branch.
12. **Create PR Summary:** Soạn thảo bản tóm tắt nội dung PR.
13. **Create Pull Request:** Tạo PR trên GitHub vào nhánh `main`.
14. **Update Review Status:** Cập nhật trạng thái ticket Jira sang **`Under Review`**.
15. **Request Review (Dastan):** Đưa cho **Dastan** review PR và theo dõi feedback. ✅ *(Kết thúc)*

---

### 🧪 2. [API-QA] - [QA-5915]: Automate BE - Poller Entity/Strategy Parsing (`GTO-15105`)

> [!NOTE]
> **Nhận diện loại Ticket:** Chứa từ khóa `endpoint` và tag `[API-QA]` ➔ Tự động áp dụng **Quy trình chuẩn 15 bước (Endpoint Automation Workflow)**.
> ⚙️ **Testing endpoint = sửa in project** (implement test code trực tiếp trong project repository).

1. **Create Test Scenario Prompt:** Tạo prompt yêu cầu AI phân tích test steps và tự động sinh Scenarios cho Poller Entity/Strategy Parsing.
2. **Create New Branch:** Tạo branch độc lập và chuyển status ticket sang `In Project`.
3. **Complete Scenario:** Rà soát và hoàn thiện các kịch bản kiểm thử (Happy Path, Negative Cases, Schema validation) trước khi viết code.
4. **Review Code:** Tự review code triển khai test script và các câu lệnh assert.
5. **Run Code:** Chạy test suite automation cục bộ (Pass 100%).
6. **Fix Code:** Sửa các lỗi phát sinh về assertion và kiểu dữ liệu trả về (nếu có).
7. **Create TestRail:** Tạo và cập nhật test cases lên hệ thống TestRail.
8. **Run Review Code:** Chạy lại toàn bộ test suite sau khi tạo TestRail để đảm bảo không có regression (Pass 100%).
9. **Check Orphan Functions:** Kiểm tra và dọn dẹp các orphan functions sau khi hoàn thành.
   > 💡 Sử dụng lệnh `/review-code` để AI tự động scan orphan functions.
10. **Kiểm tra model Pydantic:** Rà soát lại toàn bộ model Pydantic đảm bảo đúng schema.
11. **Commit and Push Code:** Commit với message chuẩn và push code lên remote branch.
12. **Create PR Summary:** Soạn thảo bản tóm tắt nội dung PR.
13. **Create Pull Request:** Tạo PR trên GitHub vào nhánh `main`.
14. **Update Review Status:** Cập nhật trạng thái ticket Jira sang **`Under Review`**.
15. **Request Review (Dastan):** Đưa cho **Dastan** review PR và theo dõi feedback. ✅ *(Kết thúc)*

---

## 💡 Ghi nhớ / Ideas

- Làm xong nhớ check orphan functions here

---

*Tạo tự động vào lúc 08:01:30*
