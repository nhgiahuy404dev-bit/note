# 📝 Ghi chú ngày 18/09/2026 (Thứ Sáu)

---

## 🎯 Mục tiêu trong ngày

- [ ] **[TEST EXECUTION] - [QA-6885] [GTO-16167] [GTO-16282]:** Alert on Transfer Tool when instruction are pending approval

---

## 📝 Ghi chú công việc / Study

### 📋 1. [TEST EXECUTION] - [QA-6885] [GTO-16167] [GTO-16282]: Alert on Transfer Tool when instruction are pending approval

> [!NOTE]
> **Nhận diện loại Ticket:** Chứa tag `[TEST EXECUTION]` ➔ Tự động áp dụng **Quy trình chuẩn 5 giai đoạn (Feature/UI Testing Workflow)** & Quy trình thực thi AI.

#### 🔄 Quy trình chuẩn 5 giai đoạn:
1. **Read Request:** Đọc và phân tích kỹ tài liệu yêu cầu Jira (`QA-6885`, `GTO-16167`, `GTO-16282`), Confluence spec và trao đổi trên Slack về cơ chế Nav Badge cảnh báo lệnh pending approval ➔ Cập nhật Jira sang **`In Progress`**.
2. **Designing:** Chuyển status Jira sang **`Designing`** ➔ Thiết kế bộ kịch bản kiểm thử (Badge rendering, Cap `9+`, Polling gate 60s, Scoping & Optimistic UI).
3. **Testing:** Chuyển status Jira sang **`Testing`** khi Dev deploy staging ➔ Thực thi test cases; đồng bộ test artifacts lên Confluence và khởi tạo Test Run trên TestRail (`/Create-TR-Run-From-TR-Draft`).
4. **Pass Test:** Sau khi hoàn tất kiểm thử đạt chuẩn (100% Passed) ➔ Chuyển Jira sang **`Pass Test`**, đánh dấu hoàn thành ✅.
5. **Create PR:** Tạo PR trên Git (nếu có automation regression script) và gán các reviewer chính: **Mohit**, **Dastan**, và **Sandeep**.

---

#### 🚀 Các bước thực hiện Test Execution chi tiết:
1. **Khởi tạo Draft qua Claude:** Lấy link Jira `QA-6885` / `GTO-16167` / `GTO-16282`, Confluence spec ➔ Chạy lệnh `/create-draft-test-execution` để AI tự động phân tích và sinh kịch bản markdown.
2. **Kiểm tra & Rà soát trên VS Code:** Mở file kịch bản trong VS Code, tự review logic các luồng: Polling gate 60s, Scoping maker-checker, hiển thị cap `9+`, ẩn khi count = 0, và regression gỡ bỏ dot cũ.
3. **Chuẩn hóa Description & Table Jira:** Dán nội dung kịch bản test vào phần Description của Jira, **bắt buộc ghi rõ Endpoint:** `GET /ui/instruction/approvals/count`.
4. **Tạo Test Artifact (`/create-test-artifact`):** **Ngay sau khi tạo ticket Jira xong**, chạy lệnh `/create-test-artifact` để đồng bộ tài liệu sang Confluence.
5. **Đánh giá Scope Ticket & Cấu hình TestRail:** Project ID `30`, phân chia Suite UI `946` và Suite E2E `947`.
6. **Gửi Dev Review trên Slack:** Nhắn tin trực tiếp cho Dev phụ trách tính năng để review và chốt Test Section trên Jira & Confluence:
   > 💬 *"Hi @dev, I have updated the detailed test scenarios for nav badge pending approval here: `<Link Jira QA-6885>`. Could you please help review the test scope? Thank you!"*
7. **Thực thi Kiểm thử (Testing) khi Dev deploy Staging:**
   - Khi Dev hoàn tất build và xác nhận deploy lên Staging ➔ Chuyển status Jira sang **`Testing`** 🚀.
   - Chạy lệnh **`/Create-TR-Run-From-TR-Draft`** để tạo Test Run trên TestRail và tiến hành thực thi kiểm thử.
8. **Nghiệm thu (Pass Test) & PR Review:** Sau khi toàn bộ test cases đạt 100% Passed ➔ Chuyển Jira sang **`Pass Test`** ✅, gán các reviewer (**Mohit**, **Dastan**, **Sandeep**).

---

## 💡 Ghi nhớ / Ideas

- **Endpoint:** `GET /ui/instruction/approvals/count` (Polling 60s).
- **Quy tắc Badge:** Đếm 1 - 9, từ 10 trở lên hiển thị `9+`, container cố định 18px chống giật layout Nav; count = 0 ẩn hẳn khỏi DOM (không hiện bare red dot).
- **Optimistic UI:** Khi bấm Approve/Reject thì badge giảm/ẩn ngay lập tức, không chờ hết chu kỳ poll 60s.
- **Polling Gate:** User không có quyền duyệt chỉ gọi 1 lần nhận `eligible: false` rồi dừng hẳn polling.
- **Suite TestRail:** Project ID `30` | Suite UI `946` | Suite E2E `947`.

---
*Tạo tự động vào lúc 17:20:53 - Cập nhật lúc 18:01:00*
