# Ghi chú ngày 22/09/2026 (Thứ Ba)

## 🎯 Mục tiêu trong ngày
- [x] **[TEST EXECUTION] - [RM-3669]:** web-reports/1.142.0 [Analytics] - Transfer tool Regression Tests (Áp dụng trọn vẹn Quy trình chuẩn Workflow 2 từ Bước 0 đến Bước 7)

---

## 📝 Ghi chú công việc / Study

- [Test Execution] - [RM - 3669] web-reports/1.142.0[Analytics] - Transfer tool Regression Tests
  + **Bước 0: Pre-check với Dastan (Bắt buộc - Chống Duplicate):**
    - Nhắn tin trực tiếp hỏi Dastan trên Slack trước khi làm ticket:
      > 💬 *"Hi Dastan, for ticket `RM-3669` (web-reports/1.142.0 [Analytics] - Transfer tool Regression Tests), have you created the draft ticket or test cases yet? If not, I will start creating the draft test execution."*
    - Nếu Dastan đã tạo ➔ Tiếp nhận bản draft của Dastan để hoàn thiện tiếp.
    - Nếu Dastan xác nhận chưa tạo ➔ Bắt đầu tiến hành Bước 1.
  + **Bước 1: Khởi tạo Draft qua Claude (`/create-draft-test-execution`):**
    - Đọc kỹ Release ticket `RM-3669`, danh sách thay đổi trong release `web-reports/1.142.0`, Confluence specs và PR diffs liên quan đến Transfer Tool.
    - Chạy slash command: `/create-draft-test-execution` để AI sinh kịch bản nháp.
    - Chuyển trạng thái ticket Jira sang **`In Progress`**.
  + **Bước 2: Rà soát & Hoàn thiện kịch bản trên VS Code:**
    - Mở file markdown kịch bản trên VS Code tự rà soát chi tiết: Precondition, Test Steps, Expected Results, Phân quyền Roles/Grants, Edge cases và các điểm chạm dùng chung (Shared Seams).
    - Đảm bảo bao phủ toàn diện các phân hệ hồi quy: Nav Badge, Polling count, Maker-Checker, Retry Failed (Ops-only), Blotter Filters/Search, Export Report, RBAC & Responsive Layout.
  + **Bước 3: Chuẩn hóa Description, Bảng Kịch Bản, Liên Kết Ticket & Đổi Trạng Thái Jira (`Designing`):**
    - Dán bảng kịch bản Markdown vào Jira Description / Test Section.
    - **Bắt buộc ghi rõ Endpoint API:** `GET /ui/instruction/approvals/count`, `POST /api/v1/transfer/instruction`, `POST /api/v1/transfer/instruction/{id}/retry`, v.v.
    - **Cấu hình Linked Work Items (Bắt buộc):** Chọn liên kết **`tests`** trỏ vào Request/Release Ticket **`RM-3669`**.
    - **Liên kết Epic cha (Bắt buộc):** Gắn vào Epic **`QA-3614: Transfer Tools Enhancement and feedback`**.
    - Chuyển trạng thái ticket Jira sang **`Designing`**.
  + **Bước 4: Tạo Test Artifact sang Confluence (`/create-test-artifact`) & Chuyển Trạng Thái Jira (`Testing` 🚀):**
    - Chạy lệnh `/create-test-artifact` ngay sau khi cập nhật Jira để đồng bộ tài liệu đặc tả kiểm thử sang Confluence.
    - Chuyển trạng thái ticket Jira sang **`Testing`** 🚀 để sẵn sàng thực thi kiểm thử.
  + **Bước 5: Thực thi Kiểm thử trên Staging & Nghiệm thu (Sign-off) Hoàn tất Ticket Jira (`Pass Test` ✅):**
    - Khi Dev build và deploy bản release `web-reports/1.142.0` lên Staging ➔ thực thi kiểm thử toàn bộ kịch bản hồi quy.
    - Chụp ảnh màn hình giao diện thực tế và lưu network log làm evidence.
    - Khi kiểm thử thực tế đạt kết quả **100% Passed** ➔ Nghiệm thu (Sign-off) chuyển trạng thái Jira sang **`Pass Test`** ✅ và tag các reviewer phụ trách (**Dastan**, **Mohit**, **Sandeep**).
  + **Bước 6: Gửi Dev Review & Nhắn Tin UAT Testing (@tiffany.kao, @jento.chan) qua Slack:**
    - ⚠️ **Nghiệm thu (Sign-off) & Hoàn tất Ticket Jira (`Pass Test` ✅) xong xuôi mới làm bước 6.**
    - **Gửi Dev Review:** Nhắn tin Slack thông báo ticket đã Pass Test và gửi link Jira/Confluence để Dev review:
      > 💬 *"Hi @dev, ticket `RM-3669` (web-reports/1.142.0 [Analytics] - Transfer tool Regression Tests) has been tested and passed (Pass Test ✅). Here is the detailed test section: `<Jira_Link>`. Could you please help review? Thank you!"*
    - **Nhắn tin UAT Testing (Ops):** Nhắn tin Slack tag đích danh **Ops** (**@tiffany.kao** và **@jento.chan**) nhờ test trên UAT theo mẫu:
      > 💬 *"hi @tiffany.kao , @jento.chan we have completed testing for RM-3669 (web-reports/1.142.0 [Analytics] - Transfer tool Regression Tests) on QA environment. And we already set it up on UAT environment, please help testing it on UAT. Thank you! 🙏"*  
      > *"UAT UI deployed at: `web-reports.uat-gp.galaxydigital.io/fund_transfer_tool`"*
    - **Soạn UAT Sign-off via Email (khi Tiffany Kao rảnh & chuẩn bị test UAT):**
      - Khi Tiffany Kao xác nhận sẵn sàng test trên UAT, QA soạn email UAT Sign-off bàn giao thông tin để Ops kiểm thử:
        * **To:** `tiffany.kao@galaxydigital.io`, `jento.chan@galaxydigital.io`
        * **Subject:** `[UAT Sign-off Request] web-reports/1.142.0 - Transfer Tool Regression Tests (RM-3669)`
        * **Mẫu Email:**
          > *"Hi Tiffany & Ops team,*  
          >  
          > *QA has completed testing for **RM-3669 (web-reports/1.142.0 [Analytics] - Transfer tool Regression Tests)** on QA environment with 100% Passed.*  
          > *The release is now deployed and ready for your UAT testing:*  
          > *- **UAT URL:** `web-reports.uat-gp.galaxydigital.io/fund_transfer_tool`*  
          > *- **Jira Ticket:** `RM-3669`*  
          > *- **Key Scope:** Transfer creation, Dual-control approval/rejection, Ops retry failed transfer, Nav alert badge & Blotter export.*  
          >  
          > *Please help test and provide your **UAT Sign-off** via this email thread once completed. Thank you! 🙏"*
  + **Bước 7: Chạy Test Run trên TestRail qua Confluence (`/create-testrail-test-run-from-confluence`):**
    - ⚠️ **Sau khi Dev đã review OK xong xuôi, lúc này mới bắt đầu làm bước 7 (TestRail).**
    - **Áp dụng cho Ticket hôm nay:** Do đây là ticket Regression Tests cho đợt release và **đã có sẵn Test Cases trên TestRail**, nên **chỉ chạy lệnh `/create-testrail-test-run-from-confluence` để tạo Test Run và ghi nhận kết quả kiểm thử (All Passed ✅)**, không cần tạo mới test cases trên TestRail.
    - Cấu hình Suite: Project ID `30`, Suite UI `946` / Suite E2E `947`.

---

## 💡 Ghi nhớ / Ideas

- 🔑 **"Hỏi Dastan trước, tạo Draft sau":** Luôn hỏi Dastan trên Slack trước khi tạo draft ticket / test cases để chống trùng lặp dữ liệu (Zero Duplication).
- 🔑 **"Jira xong là Artifact liền tay":** Cập nhật xong ticket Jira là phải chạy ngay `/create-test-artifact` để tài liệu Confluence luôn đồng bộ.
- 🔑 **"Nghiệm thu (Sign-off) First":** Chuyển Jira sang `Pass Test` ✅ xong xuôi ➔ mới làm Bước 6 (Gửi Dev Review & Tag Ops UAT) ➔ rồi mới làm Bước 7 (TestRail).
- 🔑 **"TestRail Ticket Hôm Nay (RM-3669):"** Đã có sẵn test cases trên TestRail ➔ Chỉ chạy `/create-testrail-test-run-from-confluence` chỉ run kết quả thôi không cần tạo create testrail mới.
- 🔑 **"Flow Git lấy code mới nhất về sửa:"** `git switch main` ➔ `git pull` ➔ `git fetch origin` ➔ `git switch main` ➔ `git pull origin main` ➔ `git switch <endpoint>` ➔ `git pull` ➔ `git pull origin main` (lấy code về sửa lại theo điều kiện đó).
- 🔑 **Cấu hình Jira:** Linked Work Items type `tests` ➔ `RM-3669`, Epic `QA-3614: Transfer Tools Enhancement and feedback`.
- 🔑 **Môi trường UAT:** `web-reports.uat-gp.galaxydigital.io/fund_transfer_tool` (Tag @tiffany.kao và @jento.chan).
- 🔑 **Cấu hình TestRail Suite:** Project ID `30` | API `945` | UI `946` | E2E `947`.

---
*Tạo tự động vào lúc 09:39:31*
