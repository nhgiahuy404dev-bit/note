# 📝 Ghi chú ngày 11/09/2026 (Thứ Sáu)

---

## 🎯 Mục tiêu trong ngày
- [x] **[API-QA] - [QA-6700]:** Implement automated tests for the missing Coordinator service endpoints `/instruction/by-step-ref/{uniqueRef}`
- [ ] **[API-QA] - [QA-6700]:** Implement automated tests for Coordinator service endpoints `/instruction/request/status` *(Tạm hoãn, chuyển kế hoạch sang ngày khác)*
- [ ] **[TEST EXECUTION] - [QA-6880]:** `[GTO-16245]`, `[GTO-16246]` Restrict retry of a failed transfer to ops users only, and drop the retry time window *(In Progress)*

---

## 📝 Ghi chú công việc / Study

### 🧪 1. [API-QA] - [QA-6700]: Triển khai Automated Tests cho Endpoint Coordinator
> [!NOTE]
> **Nhận diện loại Ticket:** Chứa từ khóa `endpoint` và tag `[API-QA]` ➔ Tự động áp dụng **Quy trình chuẩn 11 bước (Endpoint Automation Workflow)**:

1. **Create Test Scenario Prompt:** Tạo prompt yêu cầu AI phân tích test steps và tự động sinh Scenarios cho endpoint `/instruction/by-step-ref/{uniqueRef}`.
2. **Create New Branch:** Tạo branch độc lập `QA-6700-instruction-by-step-ref` và chuyển status ticket sang `Testing`.
3. **Complete Scenario:** Rà soát và hoàn thiện các kịch bản kiểm thử (Happy Path, Negative Cases, Schema validation) trước khi viết code.
4. **Review Code:** Tự review code triển khai test script và các câu lệnh assert.
5. **Run Code:** Chạy test suite automation cục bộ (Pass 100%).
6. **Fix Code:** Sửa các lỗi phát sinh về assertion và kiểu dữ liệu trả về (nếu có).
7. **Create TestRail:** Tạo và cập nhật test cases lên hệ thống TestRail.
8. **Commit and Push Code:** Commit với message chuẩn và push code lên remote branch.
9. **Create PR Summary:** Soạn thảo bản tóm tắt nội dung PR.
10. **Create Pull Request:** Tạo PR trên GitHub vào nhánh `main`.
11. **Update Review Status:** Cập nhật trạng thái ticket Jira sang **`Under Review`** và gán Reviewer.

---

### 📋 2. [TEST EXECUTION] - [QA-6880]: Restrict Retry of Failed Transfer
> [!NOTE]
> **Nhận diện loại Ticket:** Chứa tag `[TEST EXECUTION]` ➔ Tự động áp dụng **Quy trình chuẩn 5 giai đoạn (Feature/UI Testing Workflow)**:

1. **Read Request:** Đọc và phân tích kỹ tài liệu yêu cầu Jira `[GTO-16245]`, `[GTO-16246]`, Confluence spec và trao đổi trên Slack.
2. **Designing:** Chuyển status Jira sang `Designing` ➔ Thiết kế bộ kịch bản kiểm thử phân quyền RBAC (Ops vs Non-Ops) và kịch bản bỏ cửa sổ thời gian retry window.
3. **Testing:** Chuyển status Jira sang `Testing` khi Dev deploy staging ➔ Thực thi test Endpoint, Regression Test và E2E Test; đưa test artifacts lên Confluence.
4. **Pass Test:** Sau khi hoàn tất kiểm thử đạt chuẩn ➔ Chuyển status sang `Pass Test`, đánh dấu hoàn thành ✅.
5. **Create PR:** Tạo PR trên Git và gán các reviewer chính: **Mohit**, **Dastan**, và **Sandeep**.

---

### 🛠️ 3. Kiến thức & Kỹ thuật bổ trợ

#### 🔄 Xử lý Approval Flow trên Transfer Tool Tests (Flaky Test Fix):
- **Vấn đề:** Khi chạy test trên Jenkins, nếu môi trường QA có user hoặc manual test cấu hình `approval_required = true`, test case mong đợi `SUCCESS` sẽ bị dừng ở `PENDING_APPROVAL`, gây fail test giả.
- **Giải pháp:** Bổ sung hook test setup reset cấu hình approval về mặc định trước khi thực thi từng scenario trong `tests/transfer_gateway/features/transfer_tool.feature`.

#### 🏗️ Tổng quan về Jenkins (CI/CD System):
- Jenkins không chỉ dùng riêng để chạy test tự động (run tests) mà quản lý toàn diện quy trình build applications, deploy lên servers, thực thi scheduled jobs và tích hợp sâu với Git, Docker, Kubernetes.

---

## 💡 Ghi nhớ / Ideas
- **Quy tắc Git Branch:** Mỗi branch chỉ làm cho 1 endpoint duy nhất, cú pháp: `git checkout -b <branch-name>-<endpoint>`.
- **Nghiệm thu PR:** Luôn đảm bảo test pass 100%, đính kèm bằng chứng ảnh chụp terminal trước khi tạo PR vào `main` và chuyển ticket Jira sang `Under Review`.

---
*Tạo tự động vào lúc 08:00:02 - Cập nhật format vào lúc 18:32:00*
